# alias in bash shell
  ```
  $ vi ~/.bash_profile
  alias life1='ssh -Y ***.**.**.**'
  
  $ source ~/.bash_profile
  ```

# alias in zsh shell - MacOS
  ```
   $ vi ~/.zshrc
  alias life1='ssh -Y ***.**.**.**'
  
  $ source ~/.zshrc
  ```
